name: React CI

on: 
  push:
    branches:
      - "master"

jobs:
  build: 
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node-version: [16.14.2] 

    steps:   
      - uses: actions/checkout@v3  
      - run: npm install
      - run: npm run build
      - run: npm test
